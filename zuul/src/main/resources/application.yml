server:
  port: 9000
zuul:
  routes:
    ServiceB:
      path: /demo/**
spring:
  application:
    name: zuul-gateway
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka